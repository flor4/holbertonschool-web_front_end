# HTML Project

## 📚 Learning Objectives

At the end of this project, you should be able to explain to anyone, **without using Google**:

### 🧭 HTML Best Practices

* Understand and apply the **main guidelines** for writing clean, semantic HTML code.
* Write **W3C-compliant** code and validate it with the [W3C Validator](https://validator.w3.org/).

### 🏗️ HTML5 Page Structure

* Create the **skeleton of an HTML5 page** using proper tags (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`).
* Use **semantic HTML tags** to structure a webpage properly.

### 🧩 Semantic HTML & Layout

* Understand when to use `<div>` vs `<span>`.
* Know the **semantic roles** of:

  * `<header>`
  * `<main>`
  * `<footer>`
  * `<article>`
  * `<nav>`
  * `<section>`
  * `<aside>`

### 🔠 Headings & Content Hierarchy

* Use **headings (`<h1>` to `<h6>`)** correctly and maintain **hierarchical order** for accessibility and SEO.

### 📝 Lists & Data Representation

* Create **ordered** and **unordered** lists in HTML.
* Structure data properly using the `<table>` element.

### 🖼️ Media & Embedding

* Understand the **differences between media formats**:

  * **SVG** (vector graphics)
  * **GIF** (animations)
  * **PNG** (transparent images)
  * **JPG/JPEG** (photographs)
* Integrate **video** and **audio** content into a webpage.
* Embed **external content** such as maps, YouTube videos, or widgets using `<iframe>`.

### 🧱 Page Structure

* Build and organize a **complete HTML page** that follows semantic, accessible, and maintainable standards.

---

## 🏢 Project Context

**Techium** is the fictional company used throughout this project.
All web pages and structures will represent Techium’s online presence.

---

## ⚙️ Requirements

* A `README.md` file is **mandatory** at the root of the project.
* All code must be **valid HTML5** and pass the [W3C Validator](https://validator.w3.org/).
* Proper indentation and clear, readable structure are required.

---

## ✅ Validation

Before submission:

1. Validate all your HTML files using [W3C Validator](https://validator.w3.org/).
2. Ensure all semantic tags are used appropriately.
3. Check that all media, tables, and embedded content load correctly.

---
